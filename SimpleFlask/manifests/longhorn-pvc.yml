apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: simple-flask
  labels:
    app: simple-flask
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 512M
