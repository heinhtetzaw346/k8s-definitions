apiVersion: v1
kind: Service
metadata:
  name: springpetclinic-db
spec:
  selector:
    app: springpetclinic-db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: LoadBalancer
